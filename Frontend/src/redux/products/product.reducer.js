let initialState = {
    loading : false,
    error : false,
    payload : null
}

export const productReducer = (state = initialState, {type, payload}) => {
    switch(type){
        default : {
            return state
        }
    }
}